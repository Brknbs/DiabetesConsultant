<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.brknbs.diabetesconsultant">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".DiabetesConsultant"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".ProfileActivity" />
        <activity
            android:name=".Side_Menu_Activity"
            android:label="@string/title_activity_side__menu_" />
        <activity android:name=".DoctorLoginActivity" />
        <activity
            android:name=".DoctorPanelActivity"
            android:label="@string/title_activity_doctor_panel"
            android:theme="@style/Theme.Design.NoActionBar"/>
        <activity android:name=".MyDoctorActivity" />
        <activity android:name=".PatientInformation"></activity>
    </application>

</manifest>